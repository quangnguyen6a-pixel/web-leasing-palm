# Assets

This folder is expected to contain the approved production assets:

```
assets/
├── palm-city-logo.svg     # Palm City logo (white/light version preferred for hero use)
├── savills-logo.svg       # Savills logo — standard, unmodified
└── palm-city-hero.webp    # Approved hero perspective image
```

None of these files have been supplied yet. Until they are added, the
prototype renders visible placeholders automatically:

- `[PALM_CITY_LOGO]`
- `[SAVILLS_LOGO]`
- `[APPROVED_HERO_IMAGE]`

Drop the real files in with the exact names above and the placeholders
disappear automatically — no code changes required. See the root
`README.md` for full asset-replacement notes.
